import { MonthFormatPipe } from './month-format.pipe';

describe('MonthFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
